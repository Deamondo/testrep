1. pwd = посмотреть текущий путь
2. ls -a = посмотреть список всего что лежит в текущей папке включая скрытые 
3. ls -F = выделить папки слешом /
4. history = история команд
	history | tail -20 -> последние 20 команд
	!36 -> вызвать 36-ю команду из history
5. grep 'blablabla' filename.txt = поиск слов в файлах
	grep -R 'stime' ./ -> рекурсивно искать слово во всех файлах в текущей папке
6. grep -w = поиск слова целиком
7. cat filename = посмотреть содержимое файла
8. cat > filename  = записать в файл новое содержимое, старое удалится. Завершить Ctrl+D
9. cat >> filename = дописать в файл, старое НЕ удалится. Завершить Ctrl+D
10. вся история консольных команд хранится в файле .bash_history
11. mv old_name new_name = переименовать папку
	rm -fr /path/to/folder/name/ -> удалить папку с вложеными файлами, -f - не спрашивать подтверждения для каждого файла
	rm -r *	-> удалить все нескрытые файлы и папки в текущей папке
	rm -r .* -> удалить все скрытые в текущей папке
12. cat /var/log/dmesg|ccze -A |less -R
13. touch filename = создать файл
14. apt list code - проверка установки VSCode
15. sudo dd if=/dev/zero of=file.img bs=1M count=512 status=progress-> dd -  утилита для копирования образов и дисков в файл и из файла в любом направлении, копирование идет блоками. if - источник,  of - назначение. bs - размер блока (M = 1024 килобайт). count - количест во таких блоков
16. sudo dd if=/dev/zero of=/dev/sdb -> затрет весь диск нулями
17. ctrl + L -> очистить терминал (clear)
18. wget -> тула для закачивания файлов. wget http://www.der-hammer.info/terminal/hterm.tar.gz например
19. ls -al /dev/pts - список открытых терминалов. Можно из HTerm законнектиться к dev/pts/0 например, и получать и отправлять данные в терминал
20. sudo dmesg | grep ttyS - сообщения ядра по всем ttyS. Так можно посмотреть COM-порты
	fuser -v /opt/suts/imitVSP -> посмотреть COM-порт (в данном случае виртуальный, создан через socat), какие процессы и пользователи к нему подключены
	sudo ./tdevmon -m=/opt/suts/imitVSP -> установил эту тулу, она снифферит COM-порты. С ней kernel-модуль tdevmon.ko собран и установлен
	sudo insmod tdevmon.ko -> установка kernel-module для сниффера
	sudo cat /proc/tty/driver/serial - тоже способ
	grep serial /proc/ioports - и это тоже
21. ln -s /usr/lib/x86_64-linux-gnu/libpng16.so.16 /usr/lib/x86_64-linux-gnu/libpng12.so.0 -> создать софт линк из источника в назначение
	ln -s ~/Qt5.12.8/Tools/QtCreator/bin/qtcreator qtt -> создать ссылку на Qt. Если создать ее в usr/local/bin/, то можно вызывать из консоли из любого места
	ls -li qtt -> проверить номер иноды этой ссылки. 
	chown -h your_user:your_group qtt -> сменить права для линка
22. which qml -> посмотреть в какой папке установлена утилита
23. cd - -> прыгнуть в предыдущую директорию
24. diff ./bin ~/Hterm/hterm -s -> сравнить  файлы построчно, можно любые
25. sudo usermod -aG с $USER -> добавить юзера в группу dialout
	cat /etc/group -> список всех групп 
	groups -> посмотреть в каких я группах
	id -> тоже самое только с номерами
	sudo usermod -d /home/new-dir lemon -> изменить домашнюю папку на новую, создастся автоматически
26. cp <archive>/hterm ~/.local/bin -> чтобы hterm запускался из любой папки, надо его скопировать в local/bin. А чтобы был виден список всех портов tty, надо добавиться в группу dialout (см. 25)
27. tar -cf arch.tar ./01_github/ -> запаковать папку в архив
	tar -xvf archive.tar -> распаковать
	tar -cjvf archive.tar файлы -> сам tar не сжимает файлы а просто создает архив. А с такими флагами еще и сожмет с помощью bzip2
28. echo $PATH - показать пути по котым происходит поиск команд
29. Монтирование флэшки:
	 ps aux | grep sdc1 -> покажет процессы, файловую систему и папку куда смонтировано
	 fuser -v -m /dev/sdc1 -> эта тула тоже покажет процессы и пути. С ее помощью можно смотреть кто использует Tcp например и проч
	 sudo mount /dev/sdc1 /media/usb -> смонтировать sdc1 в папку usb (ее надо сделать заранее). Как только она вставлена то монтируется по умолчанию сразу в /media/lemon/
	 mount -> без параметров отобразит точки монтирования всех разделов, включая usb
	 lsblk -> отобразить смонтированые диски, и точки монтирования
	 systemctl -l --type mount - созданные в systemd точки монтирования
30. В nano удалить строку -> Ctrl-K
31. fuser -l -> показывает все сигналы которые эта тула может послать процессу
32. chmod a-x example.txt -> сменить права доступа. a - для всех групп (могут быть еще u, g, o). -x - отнять права на выполнение. +rw - добавить чтение -запись. Вообще права распределены на три группы - user, group, все остальные. Отображаются в виде rwx. Например rwxr-xr--.
33. sudo cat /etc/sudoers -> в строке "Defaults	secure_path" показаны пути из которых будут запускаться проги с использованием sudo. Из обычной home/.local/bin не запустится с sudo
	sudo visudo -f /etc/sudoers -> можно посмотреть файл с помощью тулы visudo, она при редактировании проверит синтаксис еще
	lemon ALL = NOPASSWD: /usr/bin/chown -> если прописать это в sudoers то ввод команды sudo chown не потребует пароля
	sudo env -> тоже можно посмотреть PATH для sudo
	разместил софтлинк на Qt в папке /usr/local/bin под названием sqt
34. df -h /usr -> можно посмотреть на каком диске физически находится папка
35. dpkg --info ../testdeb_1.0-1_all.deb -> посмотреть зависимости пакета
36. ldd 01_testQml -> посмотреть динамические библиотеки от которых зависит прога
37. fakeroot whoami -> вернет имя пользователя root, а не lemon. Команда создает виртуальную оболочку суперпользователя
38. fakeroot dpkg-deb --build supersh -> дожно быть видно корневую папку проекта (DEBIAN) - и эта команда соберет .deb, только надо подготовить остальные файлы: сам исполняемый файл, конфиги, скрипты до и после и проч.
39. lsusb -> список всех usb-устройств
	dmesg | grep usb -> сообщения системы об устройствах usb
	df -Th -> список всех подключенных томов включая usb, и покажет точки монтирования
	usb-devices -> подробная инфа обо всех usb-устройствах
	fdisk -l -> 
40. tio /dev/ttyUSB0 -> можно мониторить в терминале debug-порт.
41. lshw -> все сведения о железе, очень много. Выйти - Ctrl+t + Q
	lshw -class cpu -> инфа по отдельной категории
42. uname -r -> версия ядра
	lsb_release -a -> опреационная система (она же в /etc/lsb-release)u
	u
	
	cat /etc/issue -> одна строка об операционной системе
43. ps -> список процессов в текущей командной оболочке
	ps -eFH -> списк всех запущеных процессов, H - в виде дерева с дочерними процессами
	top -> тулу надо ставить
	htop -> еще удобней тула
44. export <NAME>='<value>' ->
	env ->
45. Esc > :qa! - выйти из Vim без сохранения
	Esc > a - начать ввод
	Esc > u - отмена действия. (4)u - 4 действия
	Esc > Ctrl+R - прменить изменения обратно
	Esc > dd - удалить строку
	Esc > v - выделять текст стрелками. 
	p — вставить содержимое буфера после курсора
	yy — копировать строку целиком, включая символ новой строки
	
46. find -L /home -samefile /home/admin/123.txt -xtype l - поиск всех софт линков, привязаных к конкретному файлу
	find ~/ -type d(f) -iname(name) "file name" -> поиск файлов(папок). Если -name без i - то будет case sensitive
	find ./01_testQml -path "./01_testQml/.git" -prune -o -type f -name "*"
	find . -type f -name "*.cpp" -o -name "*.txt" -> -o работает как логическое ИЛИ
	find . -empty -> поиск пустых файлов
	find . -size +1G -> поиск по размеру (более 1 Гб)
	find . -size -20c -> менее 20 байт
	find . -type f -mtime -3 = измененные за последние 3 дня файлы (ctime - создание, atime - доступ) (mmin, amin и cmin - это для срока в минутах)
	find . -perm a=r+w = поиск по разрешениям файлов
47. ssh-keygen - сгенерировать пару ключей. Они лягут в ~/.ssh/. Там будет два файла, один из них публичный
48. 
49. в терминале поиск по справке можно так: вывести справку чеерез man, потом Shift+/, и начать вводить слово для поиска
50. Ctrl+S -> быстрый поиск файла или каталога в MC
	ctrl-x на файле слева, затем ctrl-x,d на файле справа - сравнить два файла
	alt+i - открыть текущий каталог в соседней вкладке
	ctrl+o - посмотреть что под панелями
	ctrl+r - обновить содержимое панелей
	
51. ifconfig eth0 up/down - включить / отключить интерфейс
	sudo lshw -class network - посмотреть интерфейс через который интернет пашет
	sudo apt-get install ethtool - дополнительные настройки сетевых карт (изменения не сохраняются после перезагрузки. Можно добавить команды в etc/network/interfaces)
	
52. ssh rmms@192.168.86.141 - подключиться к виртуалке киоск через ssh. Дальше ввести пароль
	scp 111.aaa rmms@192.168.86.141:~/ -  secure copy, копирование через ssh	

53. journalctl --user -u rmms-buran-back - посмотреть журнал по какому нибудь сервису
	ldd -v buranBACK
	ldd -v /lib/x86_64-linux-gnu/libstdc++.so.6
	strings /usr/lib/x86_64-linux-gnu/libstdc++.so.6 | grep GLIBCXX
переименовал /usr/lib/x86_64-linux-gnu/libstdc++.so.6

54. Ctrl+E, O - переключиться на следующее окно в Qt
	Ctrl+E, I - переключиться на предыдущее окно в Qt
55. Ctrl+E, F4 - открыть в соседнем сплит окне заголовочник для текущего исходника, или наоборот
56. gsettings list-keys org.gnome.desktop.wm.keybindings switch-input-source - например здесь можно настроить сочетание клавишь для переключение языка. Из стандартных настроек не все комбинации можно настроить
	dconf-editor - тоже самое можно настроить через эту тулу в графическом виде
57.




это просто тестирование тега в гите!!!!
//---------------------------------------------------------------------------------------------------------------------------

#define DI_LOG_TEST - логи пишутся по другому пути, /home/lemon/suts/log

1. CustomLogger, isLogActive = false -не запускается таймер writeTimer на 1 секунду, по которому пишутся логи
2. constants.hpp - константы, пути всякие
	#define APS_LIST_PATH "/opt/suts/settings/aps_list.csv"
	#define LOG_DISK_LABEL "SUTS_LOG_DISK" 
	enum ApsSystemState {} - состояние АПСок
	#define METEOR_VERSION 2	- номер корпуса
		#define PULTS_VERSION 2	- и связанный с ним номер пультов
3. static void registerMetatypes(); - Метод регистрируут константы в мета-объектной системе Qt
4. /opt/suts/settings/settings.ini - настройки из гуя, которые передаются с одной моксы на другую


PulseAudio:
//---------------------------------------------------------------------------------------------------
/etc/pulse/client.conf -> добавил autospawn = no
удалил ~/.config/pulse

установил sudo apt install libasound2-dev
/usr/include/alsa - инклуды
/usr/share/alsa/alsa.conf -> 	defaults.ctl.card 0   переключить в 	defaults.ctl.card 1
				defaults.pcm.card 0			defaults.pcm.card 1
journalctl -> системные логи
D-Feet
aplay -l
aplay -L

//--------------------------------------------------------------------------------------------------
VSCode:
1. Ctrl + G - перепрыгнуть на нужную строку
2. Alt+Shift+- переход на предыдущее место
3. Ctrl+Shift+- переход на следующее место в коде
4. Ctrl+Shift+H - заменит во всех открытых файлах
5. Shift+Del (Ctrl+Shift+K) - удалить строку
6. Shift+F8 - перейти к следующей ошибке
7. Ctrl + 1,2,3 - переходить межды редакторами
8. Alt + 1,2,3 - переходить между вкладками


Акос:
1. лекция 5: 
	- qemu-arm -g 1234 ./a.out - запуск gdb-сервера в эмуляции arm-процессора. Ждет подключения удаленного gdb 
	- gdb
	 - file a.out
	  - set sysroot ~/arm-sysroot/
	   - set architecture armv7
	    - b main - точка останова на main. 
	     - target remote localhost:1234 - подключиться к удаленному gdbserver.
	      - continue
	       - layout src
	       - layout asm
	       - layout regs
	        -next - Дальше можно прыгать по коду (или n)

